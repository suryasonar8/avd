// Local development datasource. Swapped for prisma/schema.prod.prisma
// during the production Docker build — see Dockerfile.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:dev.sqlite"
}
